---
title: Resizable
description: Accessible resizable panel groups and layouts with keyboard support.
component: true
---

<ComponentPreview name="resizable-demo" previewClassName="h-80" />

## About

The `Resizable` component is built on top of [Corvu Resizable](https://corvu.dev/docs/primitives/resizable).

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```bash
npx solidui-cli@latest add resizable
```

</TabsContent>

<TabsContent value="manual">

<Steps class="mb-0 pt-2">

<Step>Install the following dependency:</Step>

```bash
npm install @corvu/resizable
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="resizable" title="components/ui/resizable.tsx" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx
import { ResizableHandle, ResizablePanel, ResizablePanelGroup } from "~/components/ui/resizable"
```

```tsx showLineNumbers
<ResizablePanelGroup orientation="horizontal">
  <ResizablePanel initialSize={0.3}>One</ResizablePanel>
  <ResizableHandle />
  <ResizablePanel initialSize={0.7}>Two</ResizablePanel>
</ResizablePanelGroup>
```

## Composition

Use the following composition to build a `ResizablePanelGroup`:

```text
ResizablePanelGroup
├── ResizablePanel
├── ResizableHandle
└── ResizablePanel
```

## Examples

### Vertical

Use `orientation="vertical"` for vertical resizing.

<ComponentPreview name="resizable-vertical" />

### Handle

Use the `withHandle` prop on `ResizableHandle` to show a visible handle.

<ComponentPreview name="resizable-handle" />

## API Reference

### Core Parts

| Part                  | Description                                               |
| --------------------- | --------------------------------------------------------- |
| `ResizablePanelGroup` | Flex container that manages panel layout and orientation. |
| `ResizablePanel`      | A resizable section with configurable size constraints.   |
| `ResizableHandle`     | Draggable separator between adjacent panels.              |

See the [Corvu Resizable](https://corvu.dev/docs/primitives/resizable) documentation for more information.
