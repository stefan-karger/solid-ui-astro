---
title: Input
description: Captures single-line text and other native input values.
component: true
---

<ComponentPreview name="input-demo" previewClassName="*:max-w-xs" />

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```bash
npx solidui-cli@latest add input
```

</TabsContent>

<TabsContent value="manual">

<Steps class="mb-0 pt-2">

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="input" title="components/ui/input.tsx" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx
import { Input } from "~/components/ui/input"
```

```tsx
<Input placeholder="Email" type="email" />
```

## Examples

### Basic

<ComponentPreview name="input-basic" previewClassName="*:max-w-xs" />

### Field

Use a label and helper text to provide context for the input value.

<ComponentPreview name="input-field" previewClassName="*:max-w-xs" />

### Field Group

Use `FieldGroup` patterns to show multiple fields in one form section.

<ComponentPreview name="input-fieldgroup" previewClassName="*:max-w-xs" />

### Disabled

Use the `disabled` prop to prevent interaction.

<ComponentPreview name="input-disabled" previewClassName="*:max-w-xs" />

### Invalid

Use `aria-invalid="true"` when validation fails.

<ComponentPreview name="input-invalid" previewClassName="*:max-w-xs" />

### File

Use `type="file"` to create a native file picker input.

<ComponentPreview name="input-file" previewClassName="*:max-w-xs" />

### Inline

Use a horizontal layout for compact label-input pairs.

<ComponentPreview name="input-inline" previewClassName="*:max-w-xs" />

### Grid

Use a grid layout to place related inputs side by side.

<ComponentPreview name="input-grid" previewClassName="p-6" />

### Required

Use the `required` attribute to indicate mandatory fields.

<ComponentPreview name="input-required" previewClassName="*:max-w-xs" />

### Badge

Use a small badge in the label to highlight suggested fields.

<ComponentPreview name="input-badge" previewClassName="*:max-w-xs" />

### Input Group

Use `InputGroup` to attach icons, text, or actions to an input.

<ComponentPreview name="input-input-group" previewClassName="*:max-w-xs" />

### Button Group

Use `ButtonGroup` to combine quick actions with an input.

<ComponentPreview name="input-button-group" previewClassName="*:max-w-xs" />

### Form

A full form example with multiple fields and submit behavior.

<ComponentPreview name="input-form" previewClassName="h-[32rem]" />

API Reference

All native `input` attributes are forwarded to the root element.
