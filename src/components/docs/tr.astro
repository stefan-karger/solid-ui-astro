---
import { cn } from "~/lib/utils"

interface Props extends Record<string, unknown> {
  class?: string
}

const { class: className, ...props } = Astro.props as Props
---

<tr class={cn("m-0 border-b", className)} {...props}>
  <slot />
</tr>
